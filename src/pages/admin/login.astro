---
// Login unificado (PROP): el acceso al panel usa la sesión de cliente.
// Esta ruta quedó solo por compatibilidad con bookmarks: los visitantes sin
// permiso admin ya son redirigidos a /cuenta/login por el middleware; los que
// llegan acá son admins, así que van directo al panel.
export const prerender = false;
return Astro.redirect("/admin");
---
