---
import { nav, site } from "../data/site";
const path = Astro.url.pathname;
const isActive = (href: string) =>
  href === "/" ? path === "/" : path.startsWith(href);
---

<header class="sticky top-0 z-50 border-b border-marca-100 bg-crema/90 backdrop-blur">
  <div class="mx-auto flex max-w-6xl items-center justify-between gap-4 px-4 py-3">
    <a href="/" class="text-lg font-bold tracking-tight text-marca-700">{site.nombre}</a>

    <div class="flex items-center gap-3">
    <nav class="hidden gap-1 lg:flex">
      {nav.map((item) => (
        <a
          href={item.href}
          class:list={[
            "rounded-full px-3 py-1.5 text-sm font-medium transition-colors",
            isActive(item.href)
              ? "bg-marca-600 text-white"
              : "text-marca-700 hover:bg-marca-100",
          ]}
        >
          {item.label}
        </a>
      ))}
    </nav>

    <!-- Menú compacto (mobile y tablet): el nav completo aparece recién en lg. -->
    <details class="relative lg:hidden">
      <summary class="cursor-pointer list-none rounded-md border border-marca-300 px-3 py-1.5 text-sm font-medium text-marca-700">
        Menú
      </summary>
      <nav class="absolute right-0 mt-2 flex w-44 flex-col rounded-lg border border-marca-100 bg-crema p-2 shadow-lg">
        {nav.map((item) => (
          <a
            href={item.href}
            class:list={[
              "rounded-md px-3 py-2 text-sm",
              isActive(item.href) ? "bg-marca-100 font-semibold" : "hover:bg-marca-50",
            ]}
          >
            {item.label}
          </a>
        ))}
      </nav>
    </details>

    <a href="/favoritos" class="relative rounded-full p-2 text-marca-700 hover:bg-marca-100" aria-label="Favoritos">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="h-6 w-6">
        <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
      </svg>
      <span id="fav-count" class="absolute -right-0.5 -top-0.5 hidden min-w-5 rounded-full bg-marca-600 px-1 text-center text-xs font-medium text-white">0</span>
    </a>

    <a href="/cuenta" class="rounded-full p-2 text-marca-700 hover:bg-marca-100" aria-label="Mi cuenta">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="h-6 w-6">
        <path stroke-linecap="round" stroke-linejoin="round" d="M17.982 18.725A7.488 7.488 0 0 0 12 15.75a7.488 7.488 0 0 0-5.982 2.975m11.963 0a9 9 0 1 0-11.963 0m11.963 0A8.966 8.966 0 0 1 12 21a8.966 8.966 0 0 1-5.982-2.275M15 9.75a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z" />
      </svg>
    </a>

    <a href="/carrito" class="relative rounded-full p-2 text-marca-700 hover:bg-marca-100" aria-label="Carrito">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="h-6 w-6">
        <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 3h1.386c.51 0 .955.343 1.087.835l.383 1.437M7.5 14.25a3 3 0 0 0-3 3h15.75m-12.75-3h11.218c1.121-2.3 2.1-4.684 2.924-7.138a60.114 60.114 0 0 0-16.536-1.84M7.5 14.25 5.106 5.272M6 20.25a.75.75 0 1 1-1.5 0 .75.75 0 0 1 1.5 0Zm12.75 0a.75.75 0 1 1-1.5 0 .75.75 0 0 1 1.5 0Z" />
      </svg>
      <span id="cart-count" class="absolute -right-0.5 -top-0.5 hidden min-w-5 rounded-full bg-marca-600 px-1 text-center text-xs font-medium text-white">0</span>
    </a>
    </div>
  </div>
</header>

<script>
  import { contar } from "../scripts/cart";
  import { contar as contarFav } from "../scripts/favoritos";

  function badge(id: string, n: number) {
    const el = document.getElementById(id);
    if (!el) return;
    el.textContent = String(n);
    el.classList.toggle("hidden", n === 0);
  }

  const actualizarCarrito = () => badge("cart-count", contar());
  const actualizarFav = () => badge("fav-count", contarFav());

  actualizarCarrito();
  actualizarFav();
  document.addEventListener("carrito:cambio", actualizarCarrito);
  document.addEventListener("favoritos:cambio", actualizarFav);
</script>
